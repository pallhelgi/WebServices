const userInput = require('./userInput');
const commentInput = require('./commentInput');
const sessionInput = require('./sessionInput');
const theaterInput = require('./theaterInput');
const movieInput = require('./movieInput');

module.exports = `
        ${userInput}
        ${commentInput}
        ${sessionInput}
        ${theaterInput}
        ${movieInput}
`;